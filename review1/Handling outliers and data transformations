. Handling Outliers and Data Transformations (3 Marks)
Outlier Detection and Handling:

python
Copy
Edit
import numpy as np
df = df[df['subscribers'] < np.percentile(df['subscribers'], 99)]
Data Transformation (Log Scale):

python
Copy
Edit
df['log_views'] = np.log1p(df['views'])
Explanation: Handled skewed data by removing outliers and applying log transformation.

