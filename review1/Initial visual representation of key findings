Initial Visual Representation of Key Findings (4 Marks)
Visualizations:

python
Copy
Edit
import seaborn as sns
import matplotlib.pyplot as plt

# Subscribers by category
plt.figure(figsize=(12, 6))
sns.barplot(x='category', y='subscribers', data=df, estimator=np.mean)
plt.title('Average Subscribers by Category')
plt.xticks(rotation=90)
plt.show()

# Country-wise Channel Distribution
df['country'].value_counts().head(10).plot(kind='pie', autopct='%1.1f%%')
plt.title('Top 10 Countries by Channel Count')
plt.ylabel('')
plt.show()
Explanation: Presented bar plots, pie charts, and histograms to visualize key findings effectively.
